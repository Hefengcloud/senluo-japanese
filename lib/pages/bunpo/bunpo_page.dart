import 'package:flutter/material.dart';

class BunpoPage extends StatelessWidget {
  const BunpoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}