import 'package:flutter/material.dart';

class VocabularyTextbookPage extends StatelessWidget {
  const VocabularyTextbookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}