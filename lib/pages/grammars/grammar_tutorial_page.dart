import 'package:flutter/material.dart';

class GrammarTutorialPage extends StatelessWidget {
  const GrammarTutorialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('文法入門'),
      ),
    );
  }
}
