import 'package:flutter/material.dart';

class OverheadOperationPage extends StatelessWidget {
  const OverheadOperationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
