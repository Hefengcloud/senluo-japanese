import 'package:flutter/material.dart';

class VocabularyJlptPage extends StatelessWidget {
  const VocabularyJlptPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}