import 'package:flutter/material.dart';

class KeigoHomePage extends StatelessWidget {
  const KeigoHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('敬語'),
      ),
    );
  }
}
