import 'package:flutter/material.dart';

class GojuonPanelPage extends StatelessWidget {
  const GojuonPanelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gojuon'),
      ),
      body: const Placeholder(),
    );
  }
}
